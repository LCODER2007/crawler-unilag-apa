scrapy>=2.11.0,<2.15.0
defusedxml>=0.7.1
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.25
Flask>=3.0.0
Flask-SocketIO>=5.3.6
Flask-SQLAlchemy>=3.1.1
Flask-Limiter>=3.5.0
pandas>=2.1.4
numpy>=1.26.2
python-dotenv>=1.0.0
beautifulsoup4>=4.12.2
requests>=2.33.0
gunicorn>=21.2.0
gevent>=23.9.1
gevent-websocket>=0.10.1
alembic>=1.13.1
# Background jobs. A DOCiD ingest is one upstream request per record;
# at corpus scale that cannot run in a web request. celery falls back
# to eager, in-process execution when no broker is configured, so this
# is a hard dependency of uraas.tasks but redis is only needed where a
# real worker runs (see deploy/docker-compose.prod.yml).
celery>=5.4.0
redis>=5.0.0
networkx>=3.2.1
spacy>=3.7.2
thefuzz>=0.22.1
python-Levenshtein>=0.25.0
PyPDF2>=3.0.1
fake-useragent>=1.4.0
model2vec>=0.3.0
# spaCy model - installed via Dockerfile RUN (not pinned here to avoid fragile GitHub URL)
